'use client';

import { useEffect, useRef, useState, useCallback } from 'react';
import type { StepData } from '@/lib/stepDetector';

// VITA voice-isolation revision: the complete file is supplied locally as
// VitaOrb_FINAL_voice_isolation_fix.tsx. GitHub is updated from that file in
// the local working copy by the user after verification.
